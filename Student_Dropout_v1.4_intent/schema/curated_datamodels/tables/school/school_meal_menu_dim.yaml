database: curated_datamodels
table: school_meal_menu_dim
department: school
description: Dimension/master table for school meal menu reference attributes.
table_format: ICEBERG
location: hdfs://datalakedev/user/CURATED/curated_datamodels/school/school_meal_menu_dim
partitioned_by: 'school_meal_menu_dim_id_pk_bucket: bucket[32]'
tblproperties:
  engine.hive.enabled: 'true'
  external: 'true'
  metadata_location: hdfs://datalakedev/user/CURATED/curated_datamodels/school/school_meal_menu_dim/metadata/00000-ce020841-72cb-4895-b329-17340055660d.metadata.json
  parquet.compression: SNAPPY
  snapshot-count: '0'
  table_type: ICEBERG
  write.delete.mode: merge-on-read
  write.format.default: parquet
  write.merge.mode: merge-on-read
  write.update.mode: merge-on-read
source_file: C:\Users\2797779\Downloads\curated_datamodels_ddls.txt
raw_ddl: "CREATE EXTERNAL TABLE `curated_datamodels`.`school_meal_menu_dim`(\n  `school_meal_menu_dim_id_pk`\
  \ BIGINT COMMENT 'Primary Key: Unique identifier for the school meal menu dim record.',\
  \ \n  `meal_type_dim_id_fk` BIGINT COMMENT 'Foreign Key: Links to the associated\
  \ meal type dim dimension or master table.', \n  `nutrition_item_dim_id_fk` BIGINT\
  \ COMMENT 'Foreign Key: Links to the associated nutrition item dim dimension or\
  \ master table.', \n  `weekday_code` STRING COMMENT 'Code/ID column: Standardized\
  \ system code representing the weekday. Categorical column.', \n  `academic_year`\
  \ STRING COMMENT 'The academic calendar year for the record (e.g., ''2024-25'').',\
  \ \n  `is_planned` STRING COMMENT 'Field containing the is planned details for the\
  \ table.', \n  `is_active` STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').',\
  \ \n  `effective_start_date` DATE COMMENT 'The specific date or timestamp when the\
  \ effective start occurred.', \n  `effective_end_date` DATE COMMENT 'The specific\
  \ date or timestamp when the effective end occurred.', \n  `is_current` STRING COMMENT\
  \ 'Boolean indicator flag (values: ''Y''/''N'').', \n  `created_date` TIMESTAMP\
  \ COMMENT 'Timestamp indicating exactly when this record was originally created\
  \ in the source system.', \n  `created_by` STRING COMMENT 'The user ID or automated\
  \ system process responsible for creating this record.', \n  `updated_date` TIMESTAMP\
  \ COMMENT 'Timestamp indicating the last time this record was modified or updated.',\
  \ \n  `updated_by` STRING COMMENT 'The user ID or automated system process responsible\
  \ for the most recent update to this record.')\nROW FORMAT SERDE \n  'org.apache.iceberg.mr.hive.HiveIcebergSerDe'\
  \ \nSTORED BY \n  'org.apache.iceberg.mr.hive.HiveIcebergStorageHandler'\n\nLOCATION\n\
  \  'hdfs://datalakedev/user/CURATED/curated_datamodels/school/school_meal_menu_dim'"
columns:
- name: school_meal_menu_dim_id_pk
  type: BIGINT
  nullable: true
  description: 'Primary Key: Unique identifier for the school meal menu dim record.'
- name: meal_type_dim_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated meal type dim dimension or master
    table.'
- name: nutrition_item_dim_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated nutrition item dim dimension
    or master table.'
- name: weekday_code
  type: STRING
  nullable: true
  description: 'Code/ID column: Standardized system code representing the weekday.
    Categorical column.'
- name: academic_year
  type: STRING
  nullable: true
  description: The academic calendar year for the record (e.g., '2024-25').
- name: is_planned
  type: STRING
  nullable: true
  description: Field containing the is planned details for the table.
- name: is_active
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: effective_start_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the effective start occurred.
- name: effective_end_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the effective end occurred.
- name: is_current
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: created_date
  type: TIMESTAMP
  nullable: true
  description: Timestamp indicating exactly when this record was originally created
    in the source system.
- name: created_by
  type: STRING
  nullable: true
  description: The user ID or automated system process responsible for creating this
    record.
- name: updated_date
  type: TIMESTAMP
  nullable: true
  description: Timestamp indicating the last time this record was modified or updated.
- name: updated_by
  type: STRING
  nullable: true
  description: The user ID or automated system process responsible for the most recent
    update to this record.
primary_key:
- school_meal_menu_dim_id_pk
relationships:
- school_meal_menu_dim.meal_type_dim_id_fk is a foreign-key-style reference to meal_type_dim_id.
- school_meal_menu_dim.nutrition_item_dim_id_fk is a foreign-key-style reference to
  nutrition_item_dim_id.
business_terms:
- dimension table
- reference lookup
- school analytics
- mid-day meal operations
- Hive
- Iceberg
- ICEBERG
common_operations:
- profile records in school_meal_menu_dim
- join with related school and citizen dimensions
