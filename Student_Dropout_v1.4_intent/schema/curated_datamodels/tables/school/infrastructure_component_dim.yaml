database: curated_datamodels
table: infrastructure_component_dim
department: school
description: Dimension/master table for infrastructure component reference attributes.
table_format: ICEBERG
location: hdfs://datalakedev/user/CURATED/curated_datamodels/school/infrastructure_component_dim
partitioned_by: 'infrastructure_component_dim_id_pk_bucket: bucket[32]'
tblproperties:
  engine.hive.enabled: 'true'
  external: 'true'
  metadata_location: hdfs://datalakedev/user/CURATED/curated_datamodels/school/infrastructure_component_dim/metadata/00000-625b6d40-00e6-4ac2-8ed7-80da6c1f807e.metadata.json
  parquet.compression: SNAPPY
  snapshot-count: '0'
  table_type: ICEBERG
  write.delete.mode: merge-on-read
  write.format.default: parquet
  write.merge.mode: merge-on-read
  write.update.mode: merge-on-read
source_file: C:\Users\2797779\Downloads\curated_datamodels_ddls.txt
raw_ddl: "CREATE EXTERNAL TABLE `curated_datamodels`.`infrastructure_component_dim`(\n\
  \  `infrastructure_component_dim_id_pk` BIGINT COMMENT 'Primary Key: Unique identifier\
  \ for the infrastructure component dim record.', \n  `infra_category_dim_id_fk`\
  \ BIGINT COMMENT 'Foreign Key: Links to the associated infra category dim dimension\
  \ or master table.', \n  `infra_component_code` STRING COMMENT 'Code/ID column:\
  \ Standardized system code representing the infra component. Categorical column.',\
  \ \n  `infra_component_name` STRING COMMENT 'Infra component name.', \n  `unit_type`\
  \ STRING COMMENT 'Classification field defining the specific unit type.', \n  `is_functional_track`\
  \ STRING COMMENT 'Field containing the is functional track details for the table.',\
  \ \n  `is_active` STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').',\
  \ \n  `effective_start_date` DATE COMMENT 'The specific date or timestamp when the\
  \ effective start occurred.', \n  `effective_end_date` DATE COMMENT 'The specific\
  \ date or timestamp when the effective end occurred.', \n  `is_current` STRING COMMENT\
  \ 'Boolean indicator flag (values: ''Y''/''N'').', \n  `created_date` TIMESTAMP\
  \ COMMENT 'Timestamp indicating exactly when this record was originally created\
  \ in the source system.', \n  `created_by` STRING COMMENT 'The user ID or automated\
  \ system process responsible for creating this record.', \n  `updated_date` TIMESTAMP\
  \ COMMENT 'Timestamp indicating the last time this record was modified or updated.',\
  \ \n  `updated_by` STRING COMMENT 'The user ID or automated system process responsible\
  \ for the most recent update to this record.')\nROW FORMAT SERDE \n  'org.apache.iceberg.mr.hive.HiveIcebergSerDe'\
  \ \nSTORED BY \n  'org.apache.iceberg.mr.hive.HiveIcebergStorageHandler'\n\nLOCATION\n\
  \  'hdfs://datalakedev/user/CURATED/curated_datamodels/school/infrastructure_component_dim'"
columns:
- name: infrastructure_component_dim_id_pk
  type: BIGINT
  nullable: true
  description: 'Primary Key: Unique identifier for the infrastructure component dim
    record.'
- name: infra_category_dim_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated infra category dim dimension
    or master table.'
- name: infra_component_code
  type: STRING
  nullable: true
  description: 'Code/ID column: Standardized system code representing the infra component.
    Categorical column.'
- name: infra_component_name
  type: STRING
  nullable: true
  description: Infra component name.
- name: unit_type
  type: STRING
  nullable: true
  description: Classification field defining the specific unit type.
- name: is_functional_track
  type: STRING
  nullable: true
  description: Field containing the is functional track details for the table.
- name: is_active
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: effective_start_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the effective start occurred.
- name: effective_end_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the effective end occurred.
- name: is_current
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: created_date
  type: TIMESTAMP
  nullable: true
  description: Timestamp indicating exactly when this record was originally created
    in the source system.
- name: created_by
  type: STRING
  nullable: true
  description: The user ID or automated system process responsible for creating this
    record.
- name: updated_date
  type: TIMESTAMP
  nullable: true
  description: Timestamp indicating the last time this record was modified or updated.
- name: updated_by
  type: STRING
  nullable: true
  description: The user ID or automated system process responsible for the most recent
    update to this record.
primary_key:
- infrastructure_component_dim_id_pk
relationships:
- infrastructure_component_dim.infra_category_dim_id_fk is a foreign-key-style reference
  to infra_category_dim_id.
business_terms:
- dimension table
- reference lookup
- Hive
- Iceberg
- ICEBERG
common_operations:
- profile records in infrastructure_component_dim
