database: curated_datamodels
table: school_infrastructure_progress_fact
department: school
description: Fact table for school infrastructure progress measures and events.
table_format: ICEBERG
location: hdfs://datalakedev/user/CURATED/curated_datamodels/school/school_infrastructure_progress_fact
partitioned_by: 'school_infrastructure_progress_fact_id_pk_bucket: bucket[32]'
tblproperties:
  engine.hive.enabled: 'true'
  external: 'true'
  metadata_location: hdfs://datalakedev/user/CURATED/curated_datamodels/school/school_infrastructure_progress_fact/metadata/00000-e6615b76-de05-414f-863d-3eca9569b44f.metadata.json
  parquet.compression: SNAPPY
  snapshot-count: '0'
  table_type: ICEBERG
  write.delete.mode: merge-on-read
  write.format.default: parquet
  write.merge.mode: merge-on-read
  write.update.mode: merge-on-read
source_file: C:\Users\2797779\Downloads\curated_datamodels_ddls.txt
raw_ddl: "CREATE EXTERNAL TABLE `curated_datamodels`.`school_infrastructure_progress_fact`(\n\
  \  `school_infrastructure_progress_fact_id_pk` BIGINT COMMENT 'Primary Key: Unique\
  \ identifier for the school infrastructure progress fact record.', \n  `citizen_school_id_pk`\
  \ BIGINT COMMENT 'Primary Key: Unique identifier for the citizen school record.',\
  \ \n  `reporting_date_key_fk` BIGINT COMMENT 'Foreign Key: Links to the associated\
  \ reporting date key dimension or master table.', \n  `infrastructure_component_id_fk`\
  \ BIGINT COMMENT 'Foreign Key: Links to the associated infrastructure component\
  \ dimension or master table.', \n  `infrastructure_category_id_fk` BIGINT COMMENT\
  \ 'Foreign Key: Links to the associated infrastructure category dimension or master\
  \ table.', \n  `reporting_period_id_fk` BIGINT COMMENT 'Foreign Key: Links to the\
  \ associated reporting period dimension or master table.', \n  `academic_year` STRING\
  \ COMMENT 'The academic calendar year for the record (e.g., ''2024-25'').', \n \
  \ `scheme_id_fk` BIGINT COMMENT 'Foreign Key: Links to the associated scheme dimension\
  \ or master table.', \n  `work_order_no` STRING COMMENT 'The unique work order number\
  \ assigned to the school infrastructure project.', \n  `estimate_id` STRING COMMENT\
  \ 'Estimate identifier.', \n  `total_units` INT COMMENT 'The total number of units\
  \ or facilities planned/constructed in the infrastructure project.', \n  `functional_units`\
  \ INT COMMENT 'Functional units.', \n  `non_functional_units` INT COMMENT 'Non functional\
  \ units.', \n  `under_construction_units` INT COMMENT 'Under construction units.',\
  \ \n  `planned_unit_count` INT COMMENT 'Planned unit count.', \n  `completed_unit_count`\
  \ INT COMMENT 'Completed unit count.', \n  `in_progress_unit_count` INT COMMENT\
  \ 'In progress unit count.', \n  `pending_unit_count` INT COMMENT 'Pending unit\
  \ count.', \n  `physical_progress_percentage` DECIMAL(5,2) COMMENT 'Physical progress\
  \ percentage.', \n  `financial_progress_percentage` DECIMAL(5,2) COMMENT 'Financial\
  \ progress percentage.', \n  `estimated_cost_amount` DECIMAL(12,2) COMMENT 'Estimated\
  \ cost amount.', \n  `sanctioned_amount` DECIMAL(12,2) COMMENT 'Sanctioned amount.',\
  \ \n  `released_amount` DECIMAL(12,2) COMMENT 'Released amount.', \n  `utilized_amount`\
  \ DECIMAL(12,2) COMMENT 'Utilized amount.', \n  `balance_amount` DECIMAL(12,2) COMMENT\
  \ 'Balance amount.', \n  `work_status` STRING COMMENT 'System field indicating the\
  \ current work status of the record.', \n  `start_date` DATE COMMENT 'The specific\
  \ date or timestamp when the start occurred.', \n  `expected_completion_date` DATE\
  \ COMMENT 'The specific date or timestamp when the expected completion occurred.',\
  \ \n  `actual_completion_date` DATE COMMENT 'The specific date or timestamp when\
  \ the actual completion occurred.', \n  `delay_days` INT COMMENT 'The number of\
  \ days the project or benefit delivery has been delayed relative to schedule.',\
  \ \n  `delay_reason_code` STRING COMMENT 'Code/ID column: Standardized system code\
  \ representing the delay reason. Categorical column.', \n  `school_infrastructure_progress_factcol2`\
  \ STRING COMMENT 'School infrastructure progress factcol2.', \n  `is_structurally_certified_flag`\
  \ STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').', \n  `is_safety_compliant_flag`\
  \ STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').', \n  `is_functional_flag`\
  \ STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').', \n  `maintenance_required_flag`\
  \ STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').', \n  `geo_tagged_flag`\
  \ STRING COMMENT 'Boolean indicator flag (values: ''Y''/''N'').', \n  `geo_latitude`\
  \ DECIMAL(9,6) COMMENT 'Geographic latitude.', \n  `geo_longitude` DECIMAL(9,6)\
  \ COMMENT 'Geographic longitude.', \n  `last_inspection_date` DATE COMMENT 'The\
  \ specific date or timestamp when the last inspection occurred.', \n  `inspection_score`\
  \ DECIMAL(5,2) COMMENT 'Inspection score.', \n  `inspection_remarks_code` STRING\
  \ COMMENT 'Code/ID column: Standardized system code representing the inspection\
  \ remarks. Categorical column.')\nROW FORMAT SERDE \n  'org.apache.iceberg.mr.hive.HiveIcebergSerDe'\
  \ \nSTORED BY \n  'org.apache.iceberg.mr.hive.HiveIcebergStorageHandler'\n\nLOCATION\n\
  \  'hdfs://datalakedev/user/CURATED/curated_datamodels/school/school_infrastructure_progress_fact'"
columns:
- name: school_infrastructure_progress_fact_id_pk
  type: BIGINT
  nullable: true
  description: 'Primary Key: Unique identifier for the school infrastructure progress
    fact record.'
- name: citizen_school_id_pk
  type: BIGINT
  nullable: true
  description: 'Primary Key: Unique identifier for the citizen school record.'
- name: reporting_date_key_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated reporting date key dimension
    or master table.'
- name: infrastructure_component_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated infrastructure component dimension
    or master table.'
- name: infrastructure_category_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated infrastructure category dimension
    or master table.'
- name: reporting_period_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated reporting period dimension or
    master table.'
- name: academic_year
  type: STRING
  nullable: true
  description: The academic calendar year for the record (e.g., '2024-25').
- name: scheme_id_fk
  type: BIGINT
  nullable: true
  description: 'Foreign Key: Links to the associated scheme dimension or master table.'
- name: work_order_no
  type: STRING
  nullable: true
  description: The unique work order number assigned to the school infrastructure
    project.
- name: estimate_id
  type: STRING
  nullable: true
  description: Estimate identifier.
- name: total_units
  type: INT
  nullable: true
  description: The total number of units or facilities planned/constructed in the
    infrastructure project.
- name: functional_units
  type: INT
  nullable: true
  description: Functional units.
- name: non_functional_units
  type: INT
  nullable: true
  description: Non functional units.
- name: under_construction_units
  type: INT
  nullable: true
  description: Under construction units.
- name: planned_unit_count
  type: INT
  nullable: true
  description: Planned unit count.
- name: completed_unit_count
  type: INT
  nullable: true
  description: Completed unit count.
- name: in_progress_unit_count
  type: INT
  nullable: true
  description: In progress unit count.
- name: pending_unit_count
  type: INT
  nullable: true
  description: Pending unit count.
- name: physical_progress_percentage
  type: DECIMAL(5,2)
  nullable: true
  description: Physical progress percentage.
- name: financial_progress_percentage
  type: DECIMAL(5,2)
  nullable: true
  description: Financial progress percentage.
- name: estimated_cost_amount
  type: DECIMAL(12,2)
  nullable: true
  description: Estimated cost amount.
- name: sanctioned_amount
  type: DECIMAL(12,2)
  nullable: true
  description: Sanctioned amount.
- name: released_amount
  type: DECIMAL(12,2)
  nullable: true
  description: Released amount.
- name: utilized_amount
  type: DECIMAL(12,2)
  nullable: true
  description: Utilized amount.
- name: balance_amount
  type: DECIMAL(12,2)
  nullable: true
  description: Balance amount.
- name: work_status
  type: STRING
  nullable: true
  description: System field indicating the current work status of the record.
- name: start_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the start occurred.
- name: expected_completion_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the expected completion occurred.
- name: actual_completion_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the actual completion occurred.
- name: delay_days
  type: INT
  nullable: true
  description: The number of days the project or benefit delivery has been delayed
    relative to schedule.
- name: delay_reason_code
  type: STRING
  nullable: true
  description: 'Code/ID column: Standardized system code representing the delay reason.
    Categorical column.'
- name: school_infrastructure_progress_factcol2
  type: STRING
  nullable: true
  description: School infrastructure progress factcol2.
- name: is_structurally_certified_flag
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: is_safety_compliant_flag
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: is_functional_flag
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: maintenance_required_flag
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: geo_tagged_flag
  type: STRING
  nullable: true
  description: 'Boolean indicator flag (values: ''Y''/''N'').'
- name: geo_latitude
  type: DECIMAL(9,6)
  nullable: true
  description: Geographic latitude.
- name: geo_longitude
  type: DECIMAL(9,6)
  nullable: true
  description: Geographic longitude.
- name: last_inspection_date
  type: DATE
  nullable: true
  description: The specific date or timestamp when the last inspection occurred.
- name: inspection_score
  type: DECIMAL(5,2)
  nullable: true
  description: Inspection score.
- name: inspection_remarks_code
  type: STRING
  nullable: true
  description: 'Code/ID column: Standardized system code representing the inspection
    remarks. Categorical column.'
primary_key:
- school_infrastructure_progress_fact_id_pk
- citizen_school_id_pk
relationships:
- school_infrastructure_progress_fact.reporting_date_key_fk is a foreign-key-style
  reference to reporting_date_key.
- school_infrastructure_progress_fact.infrastructure_component_id_fk is a foreign-key-style
  reference to infrastructure_component_id.
- school_infrastructure_progress_fact.infrastructure_category_id_fk is a foreign-key-style
  reference to infrastructure_category_id.
- school_infrastructure_progress_fact.reporting_period_id_fk is a foreign-key-style
  reference to reporting_period_id.
- school_infrastructure_progress_fact.scheme_id_fk is a foreign-key-style reference
  to scheme_id.
business_terms:
- fact table
- event and metric analysis
- school analytics
- Hive
- Iceberg
- ICEBERG
common_operations:
- profile records in school_infrastructure_progress_fact
- aggregate measures by academic year and linked dimensions
- join with related school and citizen dimensions
